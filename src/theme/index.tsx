import colors from "./light/light-colors";
import colorsDark from "./dark/dark-colors";
import { Theme } from "./types";

export const themeDark: Theme = {
  color: colorsDark,
};

export const themeLight: Theme = {
  color: colors,
};
